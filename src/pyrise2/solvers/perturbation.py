def solve(model, order=2, scheme="partition"):
    """Solve the model using second-order (default) or first-order perturbation."""
    print(f"Solving model using {order}-order perturbation with {scheme} scheme.")
    return {"solution": "dummy"}
