def irf(solution, horizon=20, shock='eps_r', regimes="all"):
    """Compute impulse response functions given a shock."""
    print(f"Computing IRFs for shock {shock} over {horizon} periods for regimes: {regimes}.")
    return {"irfs": "dummy"}
